
            while (++primary_ray.dir.z < z_max_distance)
            {

                //if intersects with nothing then color = 0
                //if intersects with one object color = object
                //if intersects with many objects color = closest

                // intersections_c = 0;

                // if(intersections_c == sphere_c && (closer_obj != -1)){
                //     final_color =  spheres[closer_obj].color;
                //     break;
                // }else{
                //     final_color = 0;
                // }
            }